FROM node:20-slim

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .
RUN npm run build

# Default to the API server; the worker service overrides this
# with its own Start Command in Railway's settings.
CMD ["npm", "run", "start:server"]